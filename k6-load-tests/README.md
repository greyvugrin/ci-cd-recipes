# k6 Load Tests

This directory contains demo load tests using [k6](https://k6.io/) and a complementary GitHub Actions workflow for running them in CI.

See the `load-tests` directory for the tests and README with instructions on how to install the binary to run them locally, and in GitHub Actions.
